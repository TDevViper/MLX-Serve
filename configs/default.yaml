server:
  host: "0.0.0.0"
  port: 8000
  max_concurrent_requests: 1

model:
  default: "mlx-community/Qwen1.5-0.5B-Chat"
  max_tokens: 2048
  dtype: "float16"

scheduler:
  max_batch_size: 8
  max_waiting_tokens: 20000

kv_cache:
  block_size: 16
  max_blocks: 512
